module note-check

imports 
	include/DiversiaScript
	signatures
	type-project
	name-project

rules  

	editor-note: x -> <fail> x
	/*
	editor-note: n@PropRef(name, type) -> (name, $[Type is [message]])
		where 
			//<debug> n;
			<printable><type-of> n => message
	
	editor-note: n@VarRef(name) -> (n, $[Type is [message]])
		where 
			//<debug> n;
			<printable><type-of> n => message
	*/		