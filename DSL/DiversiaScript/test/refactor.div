plugin SceneManager {
	SkyMaterial = "SpaceSkyBox"
	SkyType = "lol"
	SkyEnabled = true
	SkyDrawFirst = true
	AmbientLight = Colour(0.23, 0.2, 0.3, 1)
}

/*def Test {
	component Entity {
		Material = GraphicsShape.Sphere
	}
}

on Create 
{	
	var test : Vector3 = "shit";
	var vec : Vector3 = Vector3(1.0, 2, 3);
	var quat : Quaternion = Quaternion(1, 1, 1, 1);
	var enum : ComponentType = ComponentType.Mesh;
	var test : Entity = null;
	
	vec.Normalize();
	vec.Normalize(1);
	quat.Rotate();
	quat.lol();
	
	var dummy : Int = vec.X;
	dummy = quat.Z;
}*/
